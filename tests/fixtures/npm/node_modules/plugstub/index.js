// The prettier-plugins stand-in from the world-unification design's
// appendix A: an array of plugin records whose members carry engine
// functions, exported as `any` (index.d.ts) so consumers hold ISLAND
// values under --dynamic.
export const plugins = [
  {
    languages: [{ name: "js", parsers: ["babel"] }],
    options: { semi: { category: "js", type: "boolean", default: true } },
    parsers: { babel: { parse: (t) => ({ type: "root", src: t }) } },
  },
];
export const obj = { a: 1 };
export const scalars = { n: 5, s: "hi", b: true, nul: null, und: undefined };
