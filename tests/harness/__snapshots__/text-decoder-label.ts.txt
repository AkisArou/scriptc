text-decoder-label.ts:4:13 - error SC2020: 'new TextDecoder with runtime-valued options or an unknown label' is part of the standard library types but has no scriptc lowering yet

  3 | const runtimeLabel: string = process.argv[2] ?? "utf-8";
  4 | console.log(new TextDecoder(runtimeLabel).decode(bytes));
    |             ^~~~~~~~~~~~~~~~~~~~~~~~~~~~~
  5 | 

  hint: a recognized literal WHATWG label with default options compiles

text-decoder-label.ts:7:13 - error SC2020: 'new TextDecoder with runtime-valued options or an unknown label' is part of the standard library types but has no scriptc lowering yet

  6 | // WHATWG's replacement labels deliberately fail TextDecoder construction.
  7 | console.log(new TextDecoder("replacement").decode(bytes));
    |             ^~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
  8 | 

  hint: a recognized literal WHATWG label with default options compiles