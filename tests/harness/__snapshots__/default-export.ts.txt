default-export.ts:8:1 - error SC1012: default-exporting the mutable binding 'counter' is not supported yet

  7 | let counter = 0;
  8 | export default counter;
    | ^~~~~~~~~~~~~~~~~~~~~~~
  9 | counter = 1;

  hint: Node exports a SNAPSHOT taken when the export statement runs, which is not modeled for let/var targets — export a const, or use a named export (export { x }) for a live binding