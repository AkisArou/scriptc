fenced.ts:5:13 - error SC2020: 'process.memoryUsage' is typed by @types/node but has no scriptc lowering yet

  4 |  * never a silently broken binary. */
  5 | console.log(process.memoryUsage()); // uptime/cpuUsage/resourceUsage lower now; the V8-heap report does not
    |             ^~~~~~~~~~~~~~~~~~~
  6 | const buf = Buffer.from("bytes").readBigUInt64BE(0); // from, fill, and the numeric families lower; the bigint reads fence

  hint: the type checker sees everything @types/node declares, but only the supported surface compiles (https://scriptc.dev/limitations)

fenced.ts:6:7 - error SC2001: values of type 'bigint' cannot be compiled yet (supported: number, string, boolean, arrays, Maps, Sets, RegExp, functions, classes, records, unions of those, and 'unknown')

  5 | console.log(process.memoryUsage()); // uptime/cpuUsage/resourceUsage lower now; the V8-heap report does not
  6 | const buf = Buffer.from("bytes").readBigUInt64BE(0); // from, fill, and the numeric families lower; the bigint reads fence
    |       ^~~
  7 | console.log(Buffer.poolSize);

fenced.ts:6:13 - error SC2020: 'Buffer<ArrayBuffer>.readBigUInt64BE' is typed by @types/node but has no scriptc lowering yet

  5 | console.log(process.memoryUsage()); // uptime/cpuUsage/resourceUsage lower now; the V8-heap report does not
  6 | const buf = Buffer.from("bytes").readBigUInt64BE(0); // from, fill, and the numeric families lower; the bigint reads fence
    |             ^~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
  7 | console.log(Buffer.poolSize);

  hint: the type checker sees everything @types/node declares, but only the supported surface compiles (https://scriptc.dev/limitations)

fenced.ts:7:13 - error SC2020: 'Buffer.poolSize' is typed by @types/node but has no scriptc lowering yet

  6 | const buf = Buffer.from("bytes").readBigUInt64BE(0); // from, fill, and the numeric families lower; the bigint reads fence
  7 | console.log(Buffer.poolSize);
    |             ^~~~~~~~~~~~~~~
  8 | /* setInterval lowers, and under @types/node its Timeout RETURN now maps

  hint: the type checker sees everything @types/node declares, but only the supported surface compiles (https://scriptc.dev/limitations)

fenced.ts:17:1 - error SC2020: 'Timeout.close' is typed by @types/node but has no scriptc lowering yet

  16 | timer.refresh();
  17 | timer.close();
     | ^~~~~~~~~~~~~
  18 | /* The web-platform globals ride in with @types/node (undici):

  hint: unref(), ref(), hasRef(), and refresh() are the supported Timeout methods

fenced.ts:20:7 - error SC2020: 'AbortController' is typed by @types/node but has no scriptc lowering yet

  19 |  * AbortController fences, while fetch and a typed RequestInit lower. */
  20 | const controller = new AbortController();
     |       ^~~~~~~~~~
  21 | controller.abort();

  hint: the type checker sees everything @types/node declares, but only the supported surface compiles (https://scriptc.dev/limitations)

fenced.ts:20:20 - error SC2020: 'new AbortController' is typed by @types/node but has no scriptc lowering yet

  19 |  * AbortController fences, while fetch and a typed RequestInit lower. */
  20 | const controller = new AbortController();
     |                    ^~~~~~~~~~~~~~~~~~~~~
  21 | controller.abort();

  hint: the type checker sees everything @types/node declares, but only the supported surface compiles (https://scriptc.dev/limitations)

fenced.ts:21:1 - error SC2020: 'AbortController.abort' is typed by @types/node but has no scriptc lowering yet

  20 | const controller = new AbortController();
  21 | controller.abort();
     | ^~~~~~~~~~~~~~~~
  22 | const fetchInit: RequestInit = {

  hint: the type checker sees everything @types/node declares, but only the supported surface compiles (https://scriptc.dev/limitations)

fenced.ts:28:21 - error SC2020: 'ReadableStream.from() over Set<number> iterables' is part of the standard library types but has no scriptc lowering yet

  27 | fetch("https://example.invalid/", fetchInit);
  28 | ReadableStream.from(new Set([1, 2]));
     |                     ^~~~~~~~~~~~~~~
  29 | /* Members of SUPPORTED builtin modules beyond the lowered tables: they

  hint: arrays, Uint8Array, and strings are supported — spread other synchronous iterables into an array first: [...iterable]

fenced.ts:35:1 - error SC2020: 'fs.watchFile' is typed by @types/node but has no scriptc lowering yet

  34 | import { win32 } from "path";
  35 | watchFile("x", () => {});
     | ^~~~~~~~~~~~~~~~~~~~~~~~
  36 | console.log(cpus().length);

  hint: the type checker sees everything @types/node declares, but only the supported surface compiles (https://scriptc.dev/limitations)

fenced.ts:36:13 - error SC2020: 'CpuInfo[].length' is part of the standard library types but has no scriptc lowering yet

  35 | watchFile("x", () => {});
  36 | console.log(cpus().length);
     |             ^~~~~~~~~~~~~
  37 | console.log(win32.sep);

  hint: the type checker sees the full standard library, but only the supported surface compiles (https://scriptc.dev/limitations)

fenced.ts:37:13 - error SC2020: 'PlatformPath.sep' is typed by @types/node but has no scriptc lowering yet

  36 | console.log(cpus().length);
  37 | console.log(win32.sep);
     |             ^~~~~~~~~
  38 | /* URL members beyond the supported getters (protocol/pathname/href/

  hint: the type checker sees everything @types/node declares, but only the supported surface compiles (https://scriptc.dev/limitations)

fenced.ts:43:13 - error SC2020: 'URL.hash' is typed by @types/node but has no scriptc lowering yet

  42 | const u = new URL("https://example.com/x?a=1");
  43 | console.log(u.hash);
     |             ^~~~~~
  44 | u.searchParams.get("a");

  hint: protocol, pathname, href, host, hostname, search, searchParams, and toString() are the supported URL members

fenced.ts:50:13 - error SC2020: 'deflateSync of 'string' data' is part of the standard library types but has no scriptc lowering yet

  49 | import { deflateSync, gzipSync } from "zlib";
  50 | deflateSync("data");
     |             ^~~~~~
  51 | gzipSync("data");

  hint: zlib works on Buffers: deflateSync(Buffer.from(s, "utf8"))

fenced.ts:51:1 - error SC2020: 'zlib.gzipSync' is typed by @types/node but has no scriptc lowering yet

  50 | deflateSync("data");
  51 | gzipSync("data");
     | ^~~~~~~~~~~~~~~~
  52 | /* The http2 compatibility slice's @types/node-world fences (divergence

  hint: deflateSync and inflateSync are the lowered zlib surface

fenced.ts:60:71 - error SC2020: 'a createSecureServer SNICallback of 'undefined' values' is part of the standard library types but has no scriptc lowering yet

  59 | import * as http2 from "node:http2";
  60 | http2.createSecureServer({ allowHTTP1: true, cert: "pem", key: "pem", SNICallback: undefined });
     |                                                                       ^~~~~~~~~~~~~~~~~~~~~~
  61 | http2.createSecureServer({

  hint: SNICallback lowers as a direct option or the conditional spread ...(x ? { SNICallback: x } : {}) — the callback is (servername, cb) => void with cb: (err: Error | null, ctx?: tls.SecureContext) => void

fenced.ts:65:3 - error SC2020: 'createSecureServer options with a conditional spread' is part of the standard library types but has no scriptc lowering yet

  64 |   key: "pem",
  65 |   ...(1 ? { SNICallback: undefined } : {}),
     |   ^~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
  66 | });

  hint: the lowered conditional spread is exactly ...(x ? { SNICallback: x } : {}) — the included value must BE the condition

fenced.ts:67:71 - error SC2020: 'createSecureServer option 'streamResetBurst' with a non-literal value' is part of the standard library types but has no scriptc lowering yet

  66 | });
  67 | http2.createSecureServer({ allowHTTP1: true, cert: "pem", key: "pem", streamResetBurst: 1 + 0 });
     |                                                                       ^~~~~~~~~~~~~~~~~~~~~~~
  68 | http2.connect("https://localhost");

  hint: h2 session-tuning options are accepted with literal values (settings: a literal of literals) and ignored — the allowHTTP1 lowering serves HTTP/1.1 only, which has no h2 sessions to tune

fenced.ts:74:1 - error SC2020: 'crypto.generateKeyPair' is typed by @types/node but has no scriptc lowering yet

  73 | import { createCipheriv, generateKeyPair, pbkdf2Sync, setFips } from "node:crypto";
  74 | generateKeyPair("rsa", { modulusLength: 2048 }, () => {});
     | ^~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
  75 | createCipheriv("aes-128-cbc", Buffer.alloc(16), Buffer.alloc(16));

  hint: asymmetric-key operations need a public-key stack (bignum, RSA/EC/EdDSA math) and a KeyObject value model — neither exists in the static runtime, so no faithful lowering can be small; the lowered crypto surface is hashing, randomness, and the introspection statics

fenced.ts:75:1 - error SC2020: 'crypto.createCipheriv' is typed by @types/node but has no scriptc lowering yet

  74 | generateKeyPair("rsa", { modulusLength: 2048 }, () => {});
  75 | createCipheriv("aes-128-cbc", Buffer.alloc(16), Buffer.alloc(16));
     | ^~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
  76 | pbkdf2Sync("pw", "salt", 100000, 64, "sha512");

  hint: symmetric ciphers need a cipher stack the static runtime does not vendor — the lowered crypto surface is hashing, randomness, and the introspection statics

fenced.ts:76:1 - error SC2020: 'crypto.pbkdf2Sync' is typed by @types/node but has no scriptc lowering yet

  75 | createCipheriv("aes-128-cbc", Buffer.alloc(16), Buffer.alloc(16));
  76 | pbkdf2Sync("pw", "salt", 100000, 64, "sha512");
     | ^~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
  77 | setFips(false);

  hint: key-derivation functions have no lowering yet — the lowered crypto surface is hashing, randomness, and the introspection statics

fenced.ts:77:1 - error SC2020: 'crypto.setFips' is typed by @types/node but has no scriptc lowering yet

  76 | pbkdf2Sync("pw", "salt", 100000, 64, "sha512");
  77 | setFips(false);
     | ^~~~~~~~~~~~~~
  78 | // End of the declared-but-not-lowered surface.

  hint: a compiled binary has no FIPS provider to enable, and Node itself throws on setFips(true) in a non-FIPS build — getFips() answers 0 here