fetch-static.ts:18:7 - error SC2012: a value of type 'Headers' runs in the embedded dynamic engine, which this build does not include

  17 | timed("http://localhost/b");
  18 | const headers = new Headers();
     |       ^~~~~~~
  19 | // Headers remains the dynamic-tier fence above.

  hint: build with --dynamic to run this call in the embedded engine (adds ~620KB to the binary); static builds never include it

fetch-static.ts:18:17 - error SC2020: 'new Headers' is part of the standard library types but has no scriptc lowering yet

  17 | timed("http://localhost/b");
  18 | const headers = new Headers();
     |                 ^~~~~~~~~~~~~
  19 | // Headers remains the dynamic-tier fence above.

  hint: the type checker sees the full standard library, but only the supported surface compiles (https://scriptc.dev/limitations)