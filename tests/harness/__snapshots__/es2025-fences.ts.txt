es2025-fences.ts:9:7 - error SC2020: 'ArrayBuffer' is part of the standard library types but has no scriptc lowering yet

   8 | // Resizable ArrayBuffer + transfer (ES2024).
   9 | const ab = new ArrayBuffer(8, { maxByteLength: 16 });
     |       ^~
  10 | ab.resize(16);

  hint: the type checker sees the full standard library, but only the supported surface compiles (https://scriptc.dev/limitations)

es2025-fences.ts:9:12 - error SC2020: 'new ArrayBuffer' is part of the standard library types but has no scriptc lowering yet

   8 | // Resizable ArrayBuffer + transfer (ES2024).
   9 | const ab = new ArrayBuffer(8, { maxByteLength: 16 });
     |            ^~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
  10 | ab.resize(16);

  hint: the type checker sees the full standard library, but only the supported surface compiles (https://scriptc.dev/limitations)

es2025-fences.ts:10:1 - error SC2020: 'ArrayBuffer.resize' is part of the standard library types but has no scriptc lowering yet

   9 | const ab = new ArrayBuffer(8, { maxByteLength: 16 });
  10 | ab.resize(16);
     | ^~~~~~~~~
  11 | const moved = ab.transfer();

  hint: the type checker sees the full standard library, but only the supported surface compiles (https://scriptc.dev/limitations)

es2025-fences.ts:11:7 - error SC2020: 'ArrayBuffer' is part of the standard library types but has no scriptc lowering yet

  10 | ab.resize(16);
  11 | const moved = ab.transfer();
     |       ^~~~~
  12 | 

  hint: the type checker sees the full standard library, but only the supported surface compiles (https://scriptc.dev/limitations)

es2025-fences.ts:11:15 - error SC2020: 'ArrayBuffer.transfer' is part of the standard library types but has no scriptc lowering yet

  10 | ab.resize(16);
  11 | const moved = ab.transfer();
     |               ^~~~~~~~~~~
  12 | 

  hint: the type checker sees the full standard library, but only the supported surface compiles (https://scriptc.dev/limitations)

es2025-fences.ts:14:7 - error SC2020: 'SharedArrayBuffer' is part of the standard library types but has no scriptc lowering yet

  13 | // SharedArrayBuffer growth.
  14 | const sab = new SharedArrayBuffer(8, { maxByteLength: 16 });
     |       ^~~
  15 | sab.grow(16);

  hint: the type checker sees the full standard library, but only the supported surface compiles (https://scriptc.dev/limitations)

es2025-fences.ts:14:13 - error SC2020: 'new SharedArrayBuffer' is part of the standard library types but has no scriptc lowering yet

  13 | // SharedArrayBuffer growth.
  14 | const sab = new SharedArrayBuffer(8, { maxByteLength: 16 });
     |             ^~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
  15 | sab.grow(16);

  hint: no shared-memory threads exist in a compiled program — Uint8Array is the byte storage

es2025-fences.ts:15:1 - error SC2020: 'SharedArrayBuffer.grow' is part of the standard library types but has no scriptc lowering yet

  14 | const sab = new SharedArrayBuffer(8, { maxByteLength: 16 });
  15 | sab.grow(16);
     | ^~~~~~~~
  16 | 

  hint: the type checker sees the full standard library, but only the supported surface compiles (https://scriptc.dev/limitations)

es2025-fences.ts:19:7 - error SC2009: values of type '{ async: false; value: "not-equal" | "timed-out"; } | { async: true; value: Promise<"ok" | "timed-out">; }' cannot be compiled: the union shape is supported, but its arm '{ async: false; value: "not-equal" | "timed-out"; }' does not compile

  18 | const ia = new Int32Array(4);
  19 | const waited = Atomics.waitAsync(ia, 0, 0, 1);
     |       ^~~~~~
  20 | 

  hint: the outer shape is supported — the named component is the blocker; restate it inside the compilable set (number, string, boolean, arrays, Maps, Sets, RegExp, functions, classes, records, unions of those, and 'unknown')

es2025-fences.ts:19:16 - error SC2020: 'Atomics.waitAsync' is part of the standard library types but has no scriptc lowering yet

  18 | const ia = new Int32Array(4);
  19 | const waited = Atomics.waitAsync(ia, 0, 0, 1);
     |                ^~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
  20 | 

  hint: Atomics.wait(int32Array, idx, expected, timeoutMs) is the supported Atomics surface (scriptc has no threads — wait is the synchronous-sleep idiom, and nothing else has anyone to race)

es2025-fences.ts:22:7 - error SC2020: 'Float16Array<ArrayBuffer>' is part of the standard library types but has no scriptc lowering yet

  21 | // Float16 (ES2025): the typed array, Math.f16round, DataView accessors.
  22 | const f16 = new Float16Array(4);
     |       ^~~
  23 | console.log(Math.f16round(1.337));

  hint: the type checker sees the full standard library, but only the supported surface compiles (https://scriptc.dev/limitations)

es2025-fences.ts:22:13 - error SC2020: 'new Float16Array' is part of the standard library types but has no scriptc lowering yet

  21 | // Float16 (ES2025): the typed array, Math.f16round, DataView accessors.
  22 | const f16 = new Float16Array(4);
     |             ^~~~~~~~~~~~~~~~~~~
  23 | console.log(Math.f16round(1.337));

  hint: the type checker sees the full standard library, but only the supported surface compiles (https://scriptc.dev/limitations)

es2025-fences.ts:23:13 - error SC2020: 'Math.f16round' is part of the standard library types but has no scriptc lowering yet

  22 | const f16 = new Float16Array(4);
  23 | console.log(Math.f16round(1.337));
     |             ^~~~~~~~~~~~~
  24 | const dv = new DataView(new Uint8Array(8).buffer);

  hint: the type checker sees the full standard library, but only the supported surface compiles (https://scriptc.dev/limitations)

es2025-fences.ts:25:1 - error SC2020: 'DataView<ArrayBuffer>.setFloat16' is part of the standard library types but has no scriptc lowering yet

  24 | const dv = new DataView(new Uint8Array(8).buffer);
  25 | dv.setFloat16(0, 1.5);
     | ^~~~~~~~~~~~~
  26 | 

  hint: the type checker sees the full standard library, but only the supported surface compiles (https://scriptc.dev/limitations)

es2025-fences.ts:28:7 - error SC2020: 'IteratorObject<number, undefined, unknown>' is part of the standard library types but has no scriptc lowering yet

  27 | // First-class iterator objects.
  28 | const it = Iterator.from([1, 2, 3].values());
     |       ^~
  29 | 

  hint: the type checker sees the full standard library, but only the supported surface compiles (https://scriptc.dev/limitations)

es2025-fences.ts:28:12 - error SC2020: 'Iterator.from' is part of the standard library types but has no scriptc lowering yet

  27 | // First-class iterator objects.
  28 | const it = Iterator.from([1, 2, 3].values());
     |            ^~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
  29 | 

  hint: first-class iterator objects have no lowering — iterator helpers compile as one chain on an array iterator, consumed in place: arr.values().map(f).take(n).toArray()

es2025-fences.ts:31:15 - error SC2020: 'Promise.try with 3 arguments' is part of the standard library types but has no scriptc lowering yet

  30 | // Promise.try's ...args form (close over the values instead).
  31 | const tried = Promise.try((a: number, b: number) => a + b, 1, 2);
     |               ^~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
  32 | 

  hint: the ...args form has no lowering — close over the values: Promise.try(() => f(a, b))

es2025-fences.ts:34:17 - error SC2020: 'Object.groupBy at this result type' is part of the standard library types but has no scriptc lowering yet

  33 | // Object.groupBy at a literal-union key type (a fixed-field record).
  34 | const grouped = Object.groupBy([1, 2, 3], (n) => (n % 2 === 0 ? "even" : "odd"));
     |                 ^~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
  35 | 

  hint: a string-keyed result (Partial<Record<string, T[]>>) is the supported form — a literal-union key type makes a fixed-field record: widen the callback's key type to string (numbers stringify like JS property keys)

es2025-fences.ts:39:19 - error SC2020: 'Set.prototype.union over a set-like argument of type 'Map<number, string>'' is part of the standard library types but has no scriptc lowering yet

  38 | const m = new Map<number, string>([[1, "x"]]);
  39 | const u = s.union(m);
     |                   ^
  40 | 

  hint: only a real Set lowers (the argument's has/size must be the builtins) — build one first: new Set(...)