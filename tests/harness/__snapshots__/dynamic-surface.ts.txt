dynamic-surface.ts:9:12 - error SC2012: 'Math.ceil' runs in the embedded dynamic engine, which this build does not include

   8 | // now and no longer appear here.)
   9 | const up = Math.ceil(1.2);
     |            ^~~~~~~~~~~~~~
  10 | const tau = Math.PI * 2;

  hint: build with --dynamic to run this call in the embedded engine (adds ~620KB to the binary); static builds never include it

dynamic-surface.ts:10:13 - error SC2012: 'Math.PI' runs in the embedded dynamic engine, which this build does not include

   9 | const up = Math.ceil(1.2);
  10 | const tau = Math.PI * 2;
     |             ^~~~~~~
  11 | const price = (19.99).toFixed(2);

  hint: build with --dynamic to run this call in the embedded engine (adds ~620KB to the binary); static builds never include it

dynamic-surface.ts:11:15 - error SC2012: '.toFixed()' on numbers runs in the embedded dynamic engine, which this build does not include

  10 | const tau = Math.PI * 2;
  11 | const price = (19.99).toFixed(2);
     |               ^~~~~~~~~~~~~~~~~~
  12 | const swapped = "banana".replace("an", "AN");

  hint: build with --dynamic to run this call in the embedded engine (adds ~620KB to the binary); static builds never include it

dynamic-surface.ts:12:17 - error SC2012: '.replace()' on strings runs in the embedded dynamic engine, which this build does not include

  11 | const price = (19.99).toFixed(2);
  12 | const swapped = "banana".replace("an", "AN");
     |                 ^~~~~~~~~~~~~~~~~~~~~~~~~~~~
  13 | const ch = "hello".at(0);

  hint: build with --dynamic to run this call in the embedded engine (adds ~620KB to the binary); static builds never include it

dynamic-surface.ts:13:12 - error SC2012: '.at()' on strings runs in the embedded dynamic engine, which this build does not include

  12 | const swapped = "banana".replace("an", "AN");
  13 | const ch = "hello".at(0);
     |            ^~~~~~~~~~~~~
  14 | const n = Number.parseFloat("3.14");

  hint: build with --dynamic to run this call in the embedded engine (adds ~620KB to the binary); static builds never include it

dynamic-surface.ts:14:11 - error SC2012: 'Number.parseFloat' runs in the embedded dynamic engine, which this build does not include

  13 | const ch = "hello".at(0);
  14 | const n = Number.parseFloat("3.14");
     |           ^~~~~~~~~~~~~~~~~~~~~~~~~
  15 | 

  hint: build with --dynamic to run this call in the embedded engine (adds ~620KB to the binary); static builds never include it