mixins.ts:17:10 - error SC2008: values of type 'M.(Anonymous class) & Base' cannot be compiled: this intersection resolves to no runtime shape

  16 | // one immortal class object cannot be exact there.
  17 | function makeClass() {
     |          ^~~~~~~~~
  18 |   const X = M(Base);

  hint: restate the intersection as a single interface or type literal with the combined members; mixin-produced intersections compile where the mixin chain pins one instantiation

mixins.ts:21:13 - error SC2004: uses of 'makeClass' inherit the blocker on its declaration

  20 | }
  21 | console.log(makeClass().m);
     |             ^~~~~~~~~
  22 | 

  hint: the declaration of 'makeClass' did not compile — fix the diagnostic reported there and these sites clear with it

mixins.ts:25:19 - error SC1090: mixins over the builtin class 'Error' (its construction is libCall-shaped — only program classes compose) is not supported yet

  24 | // sit under a mixin layer.
  25 | class E extends M(Error) {}
     |                   ^~~~~
  26 | console.log(new E().m);

mixins.ts:26:13 - error SC1090: constructing through a class value whose class has no lowering (the class declaration itself was rejected — see its own diagnostic) is not supported yet

  25 | class E extends M(Error) {}
  26 | console.log(new E().m);
     |             ^~~~~~~
  27 | 

mixins.ts:33:7 - error SC2008: values of type 'M.(Anonymous class) & Base' cannot be compiled: this intersection resolves to no runtime shape

  32 | const A2 = M(Base);
  33 | const a = new A1();
     |       ^
  34 | const b = new A2();

  hint: restate the intersection as a single interface or type literal with the combined members; mixin-produced intersections compile where the mixin chain pins one instantiation

mixins.ts:34:7 - error SC2008: values of type 'M.(Anonymous class) & Base' cannot be compiled: this intersection resolves to no runtime shape

  33 | const a = new A1();
  34 | const b = new A2();
     |       ^
  35 | console.log(a.m + b.m);

  hint: restate the intersection as a single interface or type literal with the combined members; mixin-produced intersections compile where the mixin chain pins one instantiation

mixins.ts:35:13 - error SC2004: uses of 'a' inherit the blocker on its declaration

  34 | const b = new A2();
  35 | console.log(a.m + b.m);
     |             ^~~
  36 | 

  hint: the declaration of 'a' did not compile — fix the diagnostic reported there and these sites clear with it

mixins.ts:39:7 - error SC2005: values of type '<T extends Ctor<object>>(B: T) => { new (...args: any[]): (Anonymous class); prototype: M.(Anonymous class); } & T' cannot be compiled: the signature keeps its type parameters, and a compiled function is always one concrete signature (generic functions monomorphize per pinned signature — declarations, methods, and never-reassigned module-scope bindings initialized with a generic arrow/function expression)

  38 | // site, so nothing exists to store.
  39 | const held = M;
     |       ^~~~
  40 | console.log(typeof held);

  hint: annotate the destination with a concrete signature (e.g. '(x: number) => number'), instantiate explicitly ('f<number>'), or call the generic function directly

mixins.ts:39:14 - error SC1090: generic functions as values without a pinned concrete signature (annotate the destination — e.g. 'const f: (x: number) => number = M' — instantiate explicitly ('M<number>'), or call 'M' directly) is not supported yet

  38 | // site, so nothing exists to store.
  39 | const held = M;
     |              ^
  40 | console.log(typeof held);

mixins.ts:40:20 - error SC2004: uses of 'held' inherit the blocker on its declaration

  39 | const held = M;
  40 | console.log(typeof held);
     |                    ^~~~
  41 | 

  hint: the declaration of 'held' did not compile — fix the diagnostic reported there and these sites clear with it

mixins.ts:47:17 - error SC1090: mixin constructors whose rest parameter does anything but forward (`super(...args)` as the first statement is the compiled shape) is not supported yet (instantiating mixin 'Sneaky' over 'Base')

  46 |     argc: number;
  47 |     constructor(...args: any[]) {
     |                 ^~~~~~~~~~~~~~
  48 |       super(...args);

mixins.ts:55:13 - error SC1090: constructing through a class value whose class has no lowering (the class declaration itself was rejected — see its own diagnostic) is not supported yet

  54 | class S extends Sneaky(Base) {}
  55 | console.log(new S().argc);
     |             ^~~~~~~
  56 | 

mixins.ts:66:9 - error SC1090: extending computed expressions are not supported yet

  65 | }
  66 | class L extends Leaky(Base) {}
     |         ^~~~~~~~~~~~~~~~~~~
  67 | console.log(new L().l);

mixins.ts:67:13 - error SC1090: constructing through a class value whose class has no lowering (the class declaration itself was rejected — see its own diagnostic) is not supported yet

  66 | class L extends Leaky(Base) {}
  67 | console.log(new L().l);
     |             ^~~~~~~
  68 | 