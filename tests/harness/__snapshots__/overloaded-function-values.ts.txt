overloaded-function-values.ts:10:7 - error SC2007: values of type '{ (x: "a"): string; (x: "b"): number; }' cannot be compiled: the type declares multiple call signatures (overloads), and a compiled function value is always one concrete signature

   9 | }
  10 | const stored = pick;
     |       ^~~~~~
  11 | console.log(stored("a"));

  hint: annotate the slot with the ONE signature the program actually calls (e.g. '(x: number) => string'), or wrap the overloaded function in a single-signature arrow

overloaded-function-values.ts:11:13 - error SC2004: uses of 'stored' inherit the blocker on its declaration

  10 | const stored = pick;
  11 | console.log(stored("a"));
     |             ^~~~~~
  12 | 

  hint: the declaration of 'stored' did not compile — fix the diagnostic reported there and these sites clear with it

overloaded-function-values.ts:15:7 - error SC2007: values of type '{ (event: "exit", code: number): void; (event: "error", err: string): void; }' cannot be compiled: the type declares multiple call signatures (overloads), and a compiled function value is always one concrete signature

  14 | declare const on: { (event: "exit", code: number): void; (event: "error", err: string): void };
  15 | const listener = on;
     |       ^~~~~~~~
  16 | console.log(listener("exit", 0));

  hint: annotate the slot with the ONE signature the program actually calls (e.g. '(x: number) => string'), or wrap the overloaded function in a single-signature arrow

overloaded-function-values.ts:15:18 - error SC1090: the reference to 'on' (a binding form with no lowering) is not supported yet

  14 | declare const on: { (event: "exit", code: number): void; (event: "error", err: string): void };
  15 | const listener = on;
     |                  ^~
  16 | console.log(listener("exit", 0));

overloaded-function-values.ts:16:13 - error SC2004: uses of 'listener' inherit the blocker on its declaration

  15 | const listener = on;
  16 | console.log(listener("exit", 0));
     |             ^~~~~~~~
  17 | 

  hint: the declaration of 'listener' did not compile — fix the diagnostic reported there and these sites clear with it